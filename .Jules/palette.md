## 2024-05-24 - Task and Checkbox Keyboard Accessibility\n**Learning:** Replaced `display: none` on native checkboxes with Tailwind `sr-only` and `peer` classes to maintain proper keyboard accessibility, focus state tracking (`peer-focus-visible`), and screen-reader support without breaking visually custom layouts.\n**Action:** Use `sr-only` on native form inputs instead of hiding them entirely when creating custom styled components, and ensure all elements with `role="button"` are focusable via `tabindex="0"` and listen for `Enter`/`Space`.
